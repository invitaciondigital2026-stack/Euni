import { getDatabase } from '@netlify/database'; const db=getDatabase();
export default async req=>{if(req.method!=='POST')return new Response('Method not allowed',{status:405});let b;try{b=await req.json()}catch{return Response.json({error:'Solicitud inválida.'},{status:400})}const codigo=String(b.codigo||'').trim().toUpperCase(),asistencia=String(b.asistencia||''),cantidad=Number(b.cantidad||0),acompanantes=String(b.acompanantes||'').trim().slice(0,1000);if(!codigo||!['si','no'].includes(asistencia))return Response.json({error:'Completá la confirmación.'},{status:400});const rows=await db.sql`SELECT id,lugares FROM invitados WHERE codigo=${codigo} LIMIT 1`;if(!rows.length)return Response.json({error:'No encontramos esta invitación.'},{status:404});const g=rows[0],confirmados=asistencia==='si'?cantidad:0;if(asistencia==='si'&&(!Number.isInteger(cantidad)||cantidad<1||cantidad>g.lugares))return Response.json({error:`La cantidad debe estar entre 1 y ${g.lugares}.`},{status:400});await db.sql`UPDATE invitados SET estado=${asistencia==='si'?'confirmado':'no_asiste'},confirmados=${confirmados},acompanantes=${acompanantes},fecha_confirmacion=NOW(),updated_at=NOW() WHERE id=${g.id}`;return Response.json({ok:true});};
export const config={path:'/.netlify/functions/rsvp'};
